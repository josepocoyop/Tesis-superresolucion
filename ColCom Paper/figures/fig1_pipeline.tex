% Fig. 1: proposed pipeline.
% Absolute coordinates are used so that the three labels underneath stay inside
% the column width, which is what constrains the drawing.
\begin{tikzpicture}[diagram]

  \singleframe{lr}{thumb_lr.png}{(0.5,0)}

  \node[block, minimum width=21mm, anchor=west] (sr) at (1.5,0)
    {\textbf{ESRGAN}\\Spatial\\super-resolution ($\times4$)\\
     {\color{black!60}45.5 ms/frame}};

  \node[block, minimum width=21mm, anchor=west] (rife) at (4.2,0)
    {\textbf{RIFE}\\Temporal\\interpolation ($\times2$)\\
     {\color{black!60}14.0 ms/frame}};

  \singleframe{out}{thumb_sr.png}{(7.25,0)}

  \draw[flow] (lr.east) -- (sr.west);
  \draw[flow] (sr.east) -- (rife.west);
  \draw[flow] (rife.east) -- (out.west);

  % the labels sit on a common baseline below the blocks, which are the lowest
  % elements, so that none of them touches a border
  \coordinate (base) at ($(sr.south)-(0,0.14)$);
  \coordinate (mid)  at ($(sr.east)!0.5!(rife.west)$);

  \node[note, anchor=north] at (lr |- base)
    {LR frames\\$212\times120$, 15 fps\\$N$ frames};
  \node[note, anchor=north] at (mid |- base)
    {$848\times480$\\15 fps\\$N$ frames};
  \node[note, anchor=north] at (out |- base)
    {Enhanced video\\$848\times480$, 30 fps\\$2N-1$ frames};

\end{tikzpicture}
